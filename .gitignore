# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
TODO
.DS_Store
browser
# Process artifacts from the agent-improvement work (kept locally, not tracked)
report.md
CHANGELOG-TMP.md
/comparison/
# Failure artifacts - screenshots on failure, plus the .actual/.diff pair a failed
# visual assertion writes.  Baselines live in a SEPARATE directory and ARE committed.
biloba-screenshots/
# The parallel-run handshake file (process 1 publishes the browser WebSocket URL here).
# Normally cleaned up; an interrupted run leaves one behind.
.biloba-config-*
