The project `make packaging-check` (scripts/packaging-check.sh) copies into an empty directory,
installs the packed npm tarballs into, type-checks, and runs: the Vitest setup the docs teach,
against one static page.  Not published.
